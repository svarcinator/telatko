MODULE main

VAR
position : {L, R};
eats : boolean;

ASSIGN
init(position) := R;
init(eats) := FALSE;
next(position) := {L, R};
next(eats):= case
	      next(position) = L : {TRUE, FALSE};
	      TRUE : FALSE;
	      esac;



LTLSPEC
         G(eats -> X(position=L));


